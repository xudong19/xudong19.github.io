to preview website in github codespace.
(This might be not needed. The 0808 port should be automatically loaded)

```
bundle exec jekyll serve --host 0.0.0.0 --port 4000 --livereload --force_polling

```

run

```
npx prettier . --write
```

before commiting
